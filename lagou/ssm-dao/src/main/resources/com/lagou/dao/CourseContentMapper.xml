<?xml version="1.0" encoding="UTF-8" ?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lagou.dao.CourseContentMapper">
<!--resultMap设置-->
    <resultMap id="BaseResultMap" type="com.lagou.domain.CourseSection">
        <id property="id" column="id"></id>
        <result property="courseId" column="course_id"></result>
        <result property="sectionName" column="section_name"></result>
        <result property="description" column="description"></result>
        <result property="createTime" column="create_time"></result>
        <result property="isDe" column="is_de"></result>
        <result property="orderNum" column="order_num"></result>
        <result property="status" column="status"></result>

        <!-- 使用 collection,配置一对多关系 -->
        <collection property="lessonList" ofType="com.lagou.domain.CourseLesson" >
            <id property="id" column="lessonId"></id>
            <result property="courseId" column="course_id"></result>
            <result property="sectionId" column="section_id"></result>
            <result property="theme" column="theme"></result>
            <result property="duration" column="duration"></result>
            <result property="isFree" column="is_free"></result>
            <result property="orderNum" column="order_num"></result>
            <result property="status" column="status"></result>

        </collection>





    </resultMap>
    

    
    <!--查看课程章节的一对多查询-->
    <select id="findSectionAndLessonByCourseId" resultMap="BaseResultMap" parameterType="int">
select cs.*, <include refid="lesson_column_list"></include>
from course_section cs left join course_lesson cl  ON cs.id = cl.section_id
WHERE cs.course_id = #{courseId} ORDER BY cs.order_num ;
    </select>


    <!-- 课时表字段信息 -->
    <sql id="lesson_column_list">
 cl.id as lessonId, cl.course_id,
cl.section_id, cl.theme,
cl.duration, cl.is_free,
cl.order_num, cl.status </sql>


    <!--根据课程章节回显课程信息-->
    <select id="findCourseByCourseId" resultType="com.lagou.domain.Course" parameterType="int">

select  id,course_name from  course where id=#{courseId};

    </select>

<!--新增章节信息-->
<insert id="saveSection" parameterType="com.lagou.domain.CourseSection">
    insert into course_section(
course_id, section_name, description, order_num,
 STATUS, create_time, update_time )
    VALUES(#{courseId},#{sectionName},#{description},#{orderNum},#{status},#{createTime},#{updateTime});
</insert>

    <!--修改课程章节-->
    <update id="updateSection" parameterType="com.lagou.domain.CourseSection">
   update course_section
        <trim prefix="SET" suffixOverrides=",">
            <if test="sectionName != null and sectionName != ''">
                section_name = #{sectionName}, </if>
            <if test="description != null and description != ''">
                description = #{description}, </if>
            <if test="orderNum != null and orderNum != '' or orderNum == 0">
                order_num = #{orderNum}, </if> <if test="updateTime != null">
            update_time=#{updateTime} </if>
        </trim> <where> <if test="id != null and id != '' ">
        id = #{id}</if>
    </where>
  /*这里要根据用户点击隐藏的id传递过来的id来修改这个课程章节*/
    </update>


    <!--新增课时-->
    <insert id="saveLesson"  parameterType="com.lagou.domain.CourseLesson">
 insert into
     course_lesson(course_id, section_id, theme, duration,
 is_free, order_num, create_time, update_time )
 VALUES(#{courseId},#{sectionId},#{theme},#{duration},#{isFree},
 #{orderNum},#{createTime},#{updateTime});

    </insert>

    <!--修改课时-->
    <update id="updateLesson" parameterType="com.lagou.domain.CourseLesson">
        update course_lesson
        <trim prefix="SET" suffixOverrides=",">

            <if test="theme != null and theme != ''">
                theme = #{theme}, </if>

            <if test="duration != null and duration != ''and duration == 0">
                duration = #{duration}, </if>

            <if test="isFree==0 or isFree==1 ">
                is_free = #{isFree}, </if>

            <if test="updateTime != null">
                update_time=#{updateTime} </if>

            <if test=" orderNum != 0">
                order_num=#{orderNum} </if>

        </trim>
         <where>   <if test="id != null and id != '' ">
        id = #{id}</if>
    </where>

    </update>


    <!--修改课程章节状态-->
    <update id="updateSectionStatus" parameterType="com.lagou.domain.CourseSection">
        update course_section set status=#{status} where id=#{id}



    </update>







</mapper>